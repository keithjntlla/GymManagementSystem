<UserControl x:Class="GymManagementSystem.Views.Settings.UserAccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1100">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="User Accounts &amp; Staff Management" 
                       Foreground="{DynamicResource ThemeFgPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,25"
                       VerticalAlignment="Center"/>

            <Button Style="{StaticResource ModernGradientButton}"
                    HorizontalAlignment="Right"
                    Click="btnAddUser_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Add New User" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </DockPanel>

        <Border Grid.Row="1" Background="{DynamicResource ThemeTableBg}" CornerRadius="15" Padding="10" Effect="{DynamicResource ThemeTableShadow}">
            <DataGrid x:Name="dgUsers" 
                      Style="{StaticResource ModernDataGrid}"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      RowHeaderWidth="0"
                      RowHeight="70" 
                      CanUserAddRows="False" 
                      SelectionMode="Single" 
                      HorizontalScrollBarVisibility="Disabled">

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Username" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Username}" VerticalAlignment="Center" 
                                           Foreground="{DynamicResource ThemeFgPrimary}" FontSize="15" Margin="10,0,0,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Role" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8" Padding="12,5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{DynamicResource ThemeRoleAdminBg}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}" Value="Staff">
                                                    <Setter Property="Background" Value="{DynamicResource ThemeRoleStaffBg}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Role}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource ThemeRoleAdminFg}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Role}" Value="Staff">
                                                        <Setter Property="Foreground" Value="{DynamicResource ThemeRoleStaffFg}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Status" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8" Padding="12,5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{DynamicResource ThemeStatusActiveBg}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                                    <Setter Property="Background" Value="{DynamicResource ThemeStatusInactiveBg}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource ThemeStatusActiveFg}" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                                        <Setter Property="Foreground" Value="{DynamicResource ThemeStatusInactiveFg}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                     <DataGridTemplateColumn Header="Actions" Width="200">
                         <DataGridTemplateColumn.CellTemplate>
                             <DataTemplate>
                                 <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                     <Button x:Name="btnEditUser" Width="70" Height="34" FontSize="13" Margin="0,0,8,0"
                                             Background="{DynamicResource ThemePanelBg}" Foreground="{DynamicResource ThemeFgPrimary}" BorderBrush="{DynamicResource ThemeInputBorder}" BorderThickness="1"
                                             VerticalAlignment="Center" Cursor="Hand" Click="btnEditUser_Click">
                                         <Button.Template>
                                             <ControlTemplate TargetType="Button">
                                                 <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                                     <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                 </Border>
                                             </ControlTemplate>
                                         </Button.Template>
                                         Edit
                                     </Button>

                                     <Button Width="90" Height="34" FontSize="13"
                                             Background="{DynamicResource ThemeNavBtnBg}" Foreground="{DynamicResource ThemeFgSecondary}" BorderThickness="0"
                                             VerticalAlignment="Center" Cursor="Hand" Click="btnToggleStatus_Click">
                                         <Button.Template>
                                             <ControlTemplate TargetType="Button">
                                                 <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                     <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                 </Border>
                                             </ControlTemplate>
                                         </Button.Template>
                                         <Button.Style>
                                             <Style TargetType="Button">
                                                 <Setter Property="Content" Value="Deactivate"/>
                                                 <Setter Property="Background" Value="{DynamicResource ThemeNavBtnBg}"/>
                                                 <Setter Property="Foreground" Value="{DynamicResource ThemeFgSecondary}"/>
                                                 <Style.Triggers>
                                                     <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                                         <Setter Property="Content" Value="Activate"/>
                                                         <Setter Property="Background" Value="#1A3D25"/>
                                                         <Setter Property="Foreground" Value="#5BE582"/>
                                                     </DataTrigger>
                                                 </Style.Triggers>
                                             </Style>
                                         </Button.Style>
                                     </Button>
                                 </StackPanel>
                             </DataTemplate>
                         </DataGridTemplateColumn.CellTemplate>
                     </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
