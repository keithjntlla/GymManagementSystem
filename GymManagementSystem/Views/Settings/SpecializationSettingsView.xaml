<UserControl x:Class="GymManagementSystem.Views.Settings.SpecializationSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Instructor Specializations" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Manage active specializations available for gym instructors" Foreground="{DynamicResource ThemeFgSecondary}" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Specializations List Card -->
            <Border Grid.Column="0" Background="{DynamicResource ThemeTableBg}" CornerRadius="15" Padding="15" Effect="{DynamicResource ThemeTableShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Active Specializations" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="16" FontWeight="SemiBold" Margin="5,0,0,15"/>

                    <DataGrid x:Name="dgSpecializations" 
                              Grid.Row="1"
                              Style="{StaticResource ModernDataGrid}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"
                              RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Specialization Name" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Actions" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="DeleteSpecialization_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Delete Specialization" VerticalAlignment="Center">
                                            <TextBlock Text="" FontFamily="Segoe UI Symbol" Foreground="#f44336" FontSize="18"/>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Add Specialization Panel -->
            <StackPanel Grid.Column="2">
                <Border Background="{DynamicResource ThemePanelBg}" CornerRadius="15" Padding="20" Effect="{DynamicResource ThemeCardShadow}">
                    <StackPanel>
                        <TextBlock Text="Add New Specialization" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>

                        <TextBlock Text="Specialization Name" Foreground="{DynamicResource ThemeFgMuted}" FontSize="13" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtSpecName" Style="{StaticResource ModernTextBox}" Tag="e.g. Strength Training" MaxLength="100" Height="45"/>
                        <TextBlock x:Name="lblSpecError" Foreground="#ff4444" FontSize="12" Margin="0,5,0,0" Visibility="Collapsed" TextWrapping="Wrap"/>

                        <Button x:Name="btnAddSpec" Style="{StaticResource ModernGradientButton}" 
                                Content="Add Specialization" 
                                Height="45" 
                                Margin="0,20,0,0" 
                                Click="AddSpecialization_Click"/>
                    </StackPanel>
                </Border>

                <!-- Information box -->
                <Border Background="{DynamicResource ThemeNoteAlertBg}" CornerRadius="10" Padding="15" Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ⓘ" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Top"/>
                        <TextBlock VerticalAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource ThemeNoteAlertFg}" FontSize="12" Width="200">
                            <Run Text="Note:" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontWeight="Bold"/>
                            <Run Text=" Adding or removing specializations will immediately update the options in the Add/Edit Instructor forms."/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Information footer -->
        <Border Grid.Row="2" Background="{DynamicResource ThemeNoteAlertBg}" CornerRadius="10" Padding="15" Margin="0,20,0,0" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ⓘ" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource ThemeNoteAlertFg}">
                    <Run Text="Guideline:" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontWeight="Bold"/>
                    <Run Text=" Make sure standard categories match the gym's services."/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
