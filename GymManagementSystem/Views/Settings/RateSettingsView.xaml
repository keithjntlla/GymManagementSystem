<UserControl x:Class="GymManagementSystem.Views.Settings.RateSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Membership Rates &amp; Plans" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,25"/>

            <Button Style="{StaticResource ModernGradientButton}"
                    HorizontalAlignment="Right"
                    Click="AddRate_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Add New Rate" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </DockPanel>

        <Border Grid.Row="1" Background="{DynamicResource ThemePanelBg}" CornerRadius="15" Padding="10">
            <DataGrid x:Name="dgRates" 
                      Style="{StaticResource ModernDataGrid}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      RowHeaderWidth="0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Plan Name" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding PlanName}" 
                                                Style="{StaticResource MembershipPlanBadge}"
                                                VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Price (₱)" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Price, StringFormat=₱\{0:N0\}}" Foreground="#4caf50" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Duration (Days)" Binding="{Binding DurationDays, StringFormat=\{0\} days}" Width="*"/>

                    <DataGridTemplateColumn Header="Actions" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Click="EditRate_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,15,0">
                                        <TextBlock Text="" FontFamily="Segoe UI Symbol" Foreground="#ff9800" FontSize="18"/>
                                    </Button>
                                    <Button Click="DeleteRate_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                        <TextBlock Text="" FontFamily="Segoe UI Symbol" Foreground="#f44336" FontSize="18"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" x:Name="ArchivedSection" Visibility="Collapsed" Margin="0,20,0,0">
            <StackPanel>
                <DockPanel Margin="0,0,0,10">
                    <TextBlock Text="Archived Plans" Foreground="{DynamicResource ThemeFgMuted}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="These plans are hidden from new sign-ups but preserved for history." 
                               Foreground="{DynamicResource ThemeFgMuted}" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </DockPanel>

                <Border Background="{DynamicResource ThemePanelBg}" CornerRadius="15" Padding="10">
                    <DataGrid x:Name="dgArchivedRates" 
                              Style="{StaticResource ModernDataGrid}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"
                              RowHeaderWidth="0"
                              MaxHeight="220">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Plan Name" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <ContentControl Content="{Binding PlanName}" 
                                                            Style="{StaticResource MembershipPlanBadge}"
                                                            Opacity="0.6"/>
                                            <Border Background="{DynamicResource ThemeCardBg}" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="ARCHIVED" Foreground="{DynamicResource ThemeFgMuted}" FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Price (₱)" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Price, StringFormat=₱\{0:N0\}}" Foreground="{DynamicResource ThemeFgMuted}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Duration (Days)" Binding="{Binding DurationDays, StringFormat=\{0\} days}" Width="*"/>

                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="RestorePlan_Click" Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                ToolTip="Restore plan to active" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="↩" FontSize="16" Foreground="#194EAF" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Restore" Foreground="#194EAF" FontSize="12" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="{DynamicResource ThemeNoteAlertBg}" CornerRadius="10" Padding="15" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ⓘ" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock VerticalAlignment="Center">
                    <Run Text="Note:" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontWeight="Bold"/>
                    <Run Text=" Changes to rates will automatically apply to all future payments. Existing active memberships will not be affected." Foreground="{DynamicResource ThemeNoteAlertFg}"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>