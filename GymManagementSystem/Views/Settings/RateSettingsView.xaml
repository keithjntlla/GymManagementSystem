<UserControl x:Class="GymManagementSystem.Views.Settings.RateSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Membership Rates &amp; Plans" Foreground="White" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <Button HorizontalAlignment="Right" 
                Padding="25,12" 
                FontWeight="Bold" 
                FontSize="15"
                Foreground="White" 
                BorderThickness="0" 
                Cursor="Hand"
                Click="AddRate_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Add New Plan" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button.Content>

                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="btnBorder" 
                                CornerRadius="20" 
                                Padding="{TemplateBinding Padding}">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#FF5722" Offset="0.0"/>
                                                <GradientStop Color="#FF1100" Offset="1.0"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.3" Color="#FF1100"/>
                                        </Border.Effect>
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="btnBorder" Property="Opacity" Value="0.9"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="btnBorder" Property="RenderTransform">
                                                <Setter.Value>
                                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </DockPanel>

        <Border Grid.Row="1" Background="#141416" CornerRadius="15" Padding="10">
            <DataGrid x:Name="dgRates" AutoGenerateColumns="False" CanUserAddRows="False" 
                      Background="Transparent" Foreground="White" BorderThickness="0"
                      RowBackground="Transparent" AlternatingRowBackground="#1a1a1c" 
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#222"
                      HeadersVisibility="Column" RowHeight="60" FontSize="15">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="Gray"/>
                        <Setter Property="Padding" Value="15,10"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#333"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="15,0" Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Plan Name" Binding="{Binding PlanName}" Width="1.5*"/>

                    <DataGridTemplateColumn Header="Price (₱)" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Price, StringFormat=₱\{0:N0\}}" Foreground="#4caf50" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Duration (Days)" Binding="{Binding DurationDays, StringFormat=\{0\} days}" Width="*"/>

                    <DataGridTemplateColumn Header="Actions" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Click="EditRate_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,15,0">
                                        <TextBlock Text="" FontFamily="Segoe UI Symbol" Foreground="#ff9800" FontSize="18"/>
                                    </Button>
                                    <Button Click="DeleteRate_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                                        <TextBlock Text="" FontFamily="Segoe UI Symbol" Foreground="#f44336" FontSize="18"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" x:Name="ArchivedSection" Visibility="Collapsed" Margin="0,20,0,0">
            <StackPanel>
                <DockPanel Margin="0,0,0,10">
                    <TextBlock Text="Archived Plans" Foreground="#888" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="These plans are hidden from new sign-ups but preserved for history." 
                               Foreground="#555" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </DockPanel>

                <Border Background="#141416" CornerRadius="15" Padding="10">
                    <DataGrid x:Name="dgArchivedRates" AutoGenerateColumns="False" CanUserAddRows="False"
                              Background="Transparent" Foreground="#888" BorderThickness="0"
                              RowBackground="Transparent" AlternatingRowBackground="#1a1a1c"
                              GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#222"
                              HeadersVisibility="Column" RowHeight="55" FontSize="14" MaxHeight="220">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#555"/>
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#2a2a2a"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="15,0" Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Plan Name" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding PlanName}" Foreground="#666" VerticalAlignment="Center"/>
                                            <Border Background="#2a2a2a" CornerRadius="4" Padding="6,2" Margin="8,0,0,0">
                                                <TextBlock Text="ARCHIVED" Foreground="#555" FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Price (₱)" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Price, StringFormat=₱\{0:N0\}}" Foreground="#555" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Duration (Days)" Binding="{Binding DurationDays, StringFormat=\{0\} days}" Width="*"/>

                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="RestorePlan_Click" Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                ToolTip="Restore plan to active">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="↩" FontSize="16" Foreground="#4090f0" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Restore" Foreground="#4090f0" FontSize="12" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="#1e2533" CornerRadius="10" Padding="15" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ⓘ" Foreground="#4090f0" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock VerticalAlignment="Center">
                    <Run Text="Note:" Foreground="#4090f0" FontWeight="Bold"/>
                    <Run Text=" Changes to rates will automatically apply to all future payments. Existing active memberships will not be affected." Foreground="#88aacc"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>