<UserControl x:Class="GymManagementSystem.Views.Settings.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000">

    <Grid Margin="0,10,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="General Gym Information" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Main Scrollable Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,0,15,0" HorizontalAlignment="Stretch">

                <!-- Gym Logo Header Section -->
                <!-- Gym Logo Header Section -->
                <Border Background="#141416" CornerRadius="10" Padding="25" Margin="0,0,0,25" HorizontalAlignment="Stretch">
                    <StackPanel>
                        <TextBlock Text="Gym Logo" Foreground="Gray" Margin="0,0,0,15" FontSize="14"/>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="110" Height="110" Background="#1a1a1c" CornerRadius="8" BorderBrush="#333" BorderThickness="1">
                                <Grid>
                                    <!-- Placeholder Icon -->
                                    <TextBlock x:Name="txtLogoPlaceholder" Text="🏢" Foreground="#333" FontSize="32" 
                               VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <!-- Only show if the Image has no source -->
                                                    <DataTrigger Binding="{Binding Source, ElementName=imgLogo}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- Actual Logo Image -->
                                    <Image x:Name="imgLogo" Stretch="Uniform" Margin="10" RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Grid>
                            </Border>

                            <StackPanel VerticalAlignment="Center" Margin="25,0,0,0">
                                <Button Height="40" Padding="25,0" Click="BtnUploadLogo_Click" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border CornerRadius="8" x:Name="border">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                        <GradientStop Color="#ff6b00" Offset="0"/>
                                                        <GradientStop Color="#ff3d00" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15,0">
                                                    <TextBlock Text="📷" Margin="0,0,10,0" Foreground="White" VerticalAlignment="Center"/>
                                                    <TextBlock Text="Upload Logo" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <TextBlock Text="Recommended size: 512x512px" Foreground="#555" Margin="0,10,0,0" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Input Fields -->
                <TextBlock Text="Gym Name" Foreground="Gray" Margin="0,0,0,8"/>
                <TextBox x:Name="txtGymName" Style="{StaticResource ModernTextBox}" Tag="Gym Name" Margin="0,0,0,20" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Address" Foreground="Gray" Margin="0,0,0,8"/>
                <TextBox x:Name="txtAddress" Style="{StaticResource ModernTextBox}" Tag="123 Street Name, City" Margin="0,0,0,20" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Contact Number" Foreground="Gray" Margin="0,0,0,8"/>
                <TextBox x:Name="txtContact" Style="{StaticResource ModernTextBox}" Tag="09XXXXXXXXX" Margin="0,0,0,20" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Email Address" Foreground="Gray" Margin="0,0,0,8"/>
                <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBox}" Tag="gymname@gmail.com" Margin="0,0,0,20" HorizontalAlignment="Stretch"/>

                <!-- Info Alert -->
                <Border Background="#1a2533" CornerRadius="10" Padding="15" Margin="0,10,0,20" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ⓘ" Foreground="#4090f0" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="This information will be used on receipts, reports, and other official documents." 
                                   Foreground="#88aacc" VerticalAlignment="Center" FontSize="13"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,10,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="lblUnsavedHint" Text="You have unsaved changes" Foreground="#ffcc00" FontSize="14" Visibility="Collapsed" Margin="0,0,20,0"/>
                <Button Content="Restore Default" Foreground="#888" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnRestore_Click"/>
            </StackPanel>
            
            <Button HorizontalAlignment="Right" Width="160" Height="45" Click="BtnSave_Click" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#00a65a" CornerRadius="8" x:Name="border">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="💾" Margin="0,0,10,0" Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock Text="Save Changes" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</UserControl>