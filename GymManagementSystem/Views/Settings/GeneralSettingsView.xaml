<UserControl x:Class="GymManagementSystem.Views.Settings.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000">

    <Grid Margin="0,10,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="General Gym Information" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,25"/>

        <!-- Main Scrollable Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,0,15,0" HorizontalAlignment="Stretch">

                <!-- Gym Logo Header Section -->
                <Border Background="{DynamicResource ThemePanelBg}" CornerRadius="10" Padding="25" Margin="0,0,0,25" HorizontalAlignment="Stretch">
                    <StackPanel>
                        <TextBlock Text="Gym Logo" Foreground="{DynamicResource ThemeFgSecondary}" Margin="0,0,0,15" FontSize="14"/>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="110" Height="110" Background="{DynamicResource ThemeInputBg}" CornerRadius="8" BorderBrush="{DynamicResource ThemeInputBorder}" BorderThickness="1">
                                <Grid>
                                    <!-- Placeholder Icon -->
                                    <TextBlock x:Name="txtLogoPlaceholder" Text="🏢" Foreground="#333" FontSize="32" 
                                                VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <!-- Only show if the Image has no source -->
                                                    <DataTrigger Binding="{Binding Source, ElementName=imgLogo}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- Actual Logo Image -->
                                    <Image x:Name="imgLogo" Stretch="Uniform" Margin="10" RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Grid>
                            </Border>

                            <StackPanel VerticalAlignment="Center" Margin="25,0,0,0">
                                
                                <Button Style="{StaticResource ModernGradientButton}"
                                        HorizontalAlignment="Right"
                                        Click="BtnUploadLogo_Click"
                                        Cursor="Hand">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📷" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Upload Logo" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button.Content>
                                </Button>

                                <TextBlock Text="Recommended size: 512x512px" Foreground="{DynamicResource ThemeFgMuted}" Margin="0,10,0,0" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Input Fields -->
                <TextBlock Text="Gym Name" Foreground="{DynamicResource ThemeFgSecondary}" Margin="0,0,0,8"/>
                <TextBox x:Name="txtGymName" Style="{StaticResource ModernTextBox}" Tag="Gym Name" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>
                <TextBlock x:Name="lblGymNameError" Foreground="#FF4444" FontSize="11" Margin="0,0,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

                <TextBlock Text="Address" Foreground="{DynamicResource ThemeFgSecondary}" Margin="0,0,0,8"/>
                <TextBox x:Name="txtAddress" Style="{StaticResource ModernTextBox}" Tag="123 Street Name, City" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>
                <TextBlock x:Name="lblAddressError" Foreground="#FF4444" FontSize="11" Margin="0,0,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

                <TextBlock Text="Contact Number" Foreground="{DynamicResource ThemeFgSecondary}" Margin="0,0,0,8"/>
                <TextBox x:Name="txtContact" Style="{StaticResource ModernTextBox}" Tag="09XXXXXXXXX" MaxLength="11" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>
                <TextBlock x:Name="lblContactError" Foreground="#FF4444" FontSize="11" Margin="0,0,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

                <TextBlock Text="Email Address" Foreground="{DynamicResource ThemeFgSecondary}" Margin="0,0,0,8"/>
                <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBox}" Tag="gymname@gmail.com" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>
                <TextBlock x:Name="lblEmailError" Foreground="#FF4444" FontSize="11" Margin="0,0,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

                <TextBlock Text="Maximum Gym Capacity" Foreground="{DynamicResource ThemeFgSecondary}" Margin="0,0,0,8"/>
                <TextBox x:Name="txtMaxCapacity" Style="{StaticResource ModernTextBox}" Tag="100" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>
                <TextBlock x:Name="lblMaxCapacityError" Foreground="#FF4444" FontSize="11" Margin="0,0,0,10" Visibility="Collapsed" TextWrapping="Wrap"/>

                <!-- Info Alert -->
                <Border Background="{DynamicResource ThemeNoteAlertBg}" CornerRadius="10" Padding="15" Margin="0,10,0,20" HorizontalAlignment="Stretch">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ⓘ" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="This information will be used on receipts, reports, and other official documents." 
                                   Foreground="{DynamicResource ThemeNoteAlertFg}" VerticalAlignment="Center" FontSize="13"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,10,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock x:Name="lblUnsavedHint" Text="You have unsaved changes" Foreground="#ffcc00" FontSize="14" Visibility="Collapsed" Margin="0,0,20,0"/>
                <Button Content="Restore Default" Foreground="{DynamicResource ThemeFgMuted}" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnRestore_Click"/>
            </StackPanel>
            
            <Button HorizontalAlignment="Right" Width="160" Height="45" Click="BtnSave_Click" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#00a65a" CornerRadius="8" x:Name="border">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="💾" Margin="0,0,10,0" Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock Text="Save Changes" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</UserControl>