<UserControl x:Class="GymManagementSystem.Views.Settings.DiscountSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Membership Tiers &amp; Discounts" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="24" FontWeight="Bold" Margin="0,0,0,25"/>

            <Button Style="{StaticResource ModernGradientButton}"
                    HorizontalAlignment="Right"
                    Click="AddDiscount_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Add Discount Tier" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </DockPanel>

        <Border Grid.Row="1" Background="{DynamicResource ThemeTableBg}" CornerRadius="15" Padding="10" Effect="{DynamicResource ThemeTableShadow}">
            <DataGrid x:Name="dgFixedDiscounts" 
                      Style="{StaticResource ModernDataGrid}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      RowHeaderWidth="0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Tier Type Group" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TargetType}" Foreground="{DynamicResource ThemeFgPrimary}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Discount Value" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8" Padding="12,5" HorizontalAlignment="Left" VerticalAlignment="Center" Background="{DynamicResource ThemeDiscountBadgeBg}">
                                    <TextBlock Text="{Binding Percentage, StringFormat=\{0\}%}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource ThemeDiscountBadgeFg}" VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Applicable Rate Scopes" Width="2.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ApplicableRates}" VerticalAlignment="Center" Foreground="#AAA" FontSize="14" Margin="5,0,0,0" TextTrimming="WordEllipsis"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Actions" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Click="EditFixedDiscount_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,15,0" ToolTip="Edit Configuration">
                                        <TextBlock Text="" FontFamily="Segoe UI Symbol" Foreground="#ff9800" FontSize="18"/>
                                    </Button>
                                    <Button Click="ArchiveFixedDiscount_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Archive Rules Matrix">
                                        <TextBlock Text="" FontFamily="Segoe UI Symbol" Foreground="#f44336" FontSize="18"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" x:Name="ArchivedSection" Visibility="Collapsed" Margin="0,20,0,0">
            <StackPanel>
                <DockPanel Margin="0,0,0,10">
                    <TextBlock Text="Archived Discount Tiers" Foreground="{DynamicResource ThemeFgMuted}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="These logic rules are hidden from registration drop-downs but preserved for metrics logs." Foreground="{DynamicResource ThemeFgMuted}" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0"/>
                </DockPanel>

                <Border Background="{DynamicResource ThemeTableBg}" CornerRadius="15" Padding="10" Effect="{DynamicResource ThemeTableShadow}">
                    <DataGrid x:Name="dgArchivedDiscounts" 
                              Style="{StaticResource ModernDataGrid}"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"
                              RowHeaderWidth="0"
                              MaxHeight="220">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Tier Type Group" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding TargetType}" Foreground="{DynamicResource ThemeFgMuted}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                            <Border Background="{DynamicResource ThemeCardBg}" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="ARCHIVED" Foreground="{DynamicResource ThemeFgMuted}" FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Discount Value" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Percentage, StringFormat=\{0\}%}" Foreground="{DynamicResource ThemeFgMuted}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Applicable Rate Scopes" Width="2.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ApplicableRates}" Foreground="{DynamicResource ThemeFgMuted}" VerticalAlignment="Center" FontSize="14" Margin="5,0,0,0" TextTrimming="WordEllipsis"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Actions" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="RestoreDiscountRule_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Restore Rule Group to Active" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="↩" FontSize="16" Foreground="#4090f0" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Restore" Foreground="#4090f0" FontSize="12" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="{DynamicResource ThemeNoteAlertBg}" CornerRadius="10" Padding="15" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ⓘ" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock VerticalAlignment="Center">
                    <Run Text="Note:" Foreground="{DynamicResource ThemeNoteAlertIconFg}" FontWeight="Bold"/>
                    <Run Text=" Modifications to dynamic tier mappings apply to all future processes. Existing logs are protected." Foreground="{DynamicResource ThemeNoteAlertFg}"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
