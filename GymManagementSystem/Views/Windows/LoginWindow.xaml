<Window x:Class="GymManagementSystem.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="660" Width="450"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">

    <Border Background="#1a191b" CornerRadius="25" BorderBrush="#333" BorderThickness="1">
        <StackPanel Margin="40">

            <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" 
                    Foreground="Gray" BorderThickness="0" Click="Close_Click"/>

            <!-- Logo: set x:Name so code-behind can swap it with the gym's custom logo -->
            <Image x:Name="imgLogo"
                   Source="/Assets/logo.png" 
                   Width="100" Height="100" 
                   HorizontalAlignment="Center" 
                   Margin="0,10,0,10"
                   RenderOptions.BitmapScalingMode="HighQuality"/>

            <!-- Gym name (big) -->
            <TextBlock x:Name="txtLoginTitle" 
                       Foreground="White" FontSize="28" 
                       FontWeight="SemiBold" HorizontalAlignment="Center" 
                       TextAlignment="Center" TextWrapping="Wrap"
                       Margin="0,0,0,6"/>

            <!-- "Staff Login" subtitle -->
            <TextBlock x:Name="txtLoginSubtitle" Text="Member Login" 
                       Foreground="#999" FontSize="15" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Username" Foreground="#999" Margin="0,0,0,8" FontSize="16"/>
            <Grid Margin="0,0,0,20">
                <TextBox x:Name="txtUsername" Background="#222123" Foreground="White" 
                         BorderBrush="#444" Padding="15,10" FontSize="16" 
                         CaretBrush="White">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <TextBlock IsHitTestVisible="False" Text="Enter your username" 
                           VerticalAlignment="Center" HorizontalAlignment="Left" 
                           Margin="15,0,0,0" Foreground="#555" FontSize="16">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=txtUsername}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <TextBlock Text="Password" Foreground="#999" Margin="0,0,0,8" FontSize="16"/>
            <PasswordBox x:Name="txtPassword" Background="#222123" Foreground="White" 
                         BorderBrush="#444" Padding="15,10" FontSize="16" CaretBrush="White">
                <PasswordBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </PasswordBox.Resources>
            </PasswordBox>

            <TextBlock Text="Forgot password?" Foreground="#ff6b00" HorizontalAlignment="Right" 
                       Margin="0,15,0,30" Cursor="Hand" FontSize="14"
                       MouseLeftButtonUp="ForgotPassword_Click"/>

            <Button Style="{StaticResource ModernGradientButton}"
                    Height="55" FontSize="18" Width="368"
                    FontWeight="Bold" Foreground="White" BorderThickness="0"
                    HorizontalAlignment="Right"
                    Content="Sign In"
                    Click="btnSignIn_Click">
            </Button>

        </StackPanel>
    </Border>
</Window>
