<UserControl x:Class="GymManagementSystem.Views.Reports.AttendanceReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.Views.Reports"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <Grid x:Name="AttendanceReportGrid" Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#141416" CornerRadius="12" Padding="12" Margin="0,0,0,15">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <Button x:Name="btnPrevDay" Content="‹" Style="{StaticResource NavButtonStyle}" Click="BtnPrevDay_Click" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpAttendanceDate" Width="160" Style="{StaticResource ModernDatePicker}" SelectedDateChanged="DpAttendanceDate_SelectedDateChanged"/>
                    <Button x:Name="btnNextDay" Content="›" Style="{StaticResource NavButtonStyle}" Click="BtnNextDay_Click" Margin="8,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="#1a1a1c" BorderBrush="#333" BorderThickness="1" CornerRadius="8" Padding="8,0" Width="280" Height="36" Margin="0,0,8,0">
                        <DockPanel VerticalAlignment="Center">
                            <TextBlock Text="🔍" FontSize="14" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,6,0" DockPanel.Dock="Left"/>
                            <TextBox x:Name="txtAttendanceSearch" Background="Transparent" BorderThickness="0" Foreground="White" CaretBrush="White" FontSize="13" VerticalAlignment="Center" Tag="Search member name or type..." TextChanged="TxtAttendanceSearch_TextChanged">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Grid>
                                                        <ScrollViewer x:Name="PART_ContentHost" Background="Transparent" VerticalAlignment="Center"/>
                                                        <TextBlock Foreground="Gray" FontSize="13" VerticalAlignment="Center" IsHitTestVisible="False" Text="{Binding Tag, RelativeSource={RelativeSource AncestorType=TextBox}}">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource AncestorType=TextBox}}" Value="">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </DockPanel>
                    </Border>
                    <Button x:Name="btnRefreshAttendance" Style="{StaticResource IconButtonStyle}" Click="BtnRefreshAttendance_Click" ToolTip="Refresh">
                        <TextBlock Text="↺" FontSize="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <UniformGrid Grid.Row="1" Columns="2" Margin="0,0,0,15">
            <Border Style="{StaticResource SummaryCardStyle}" Background="#1a2a4a" Margin="0,0,8,0">
                <DockPanel VerticalAlignment="Center">
                    <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                        <TextBlock Text="Total Visitors Today" Foreground="#8899bb" FontSize="13"/>
                        <TextBlock x:Name="lblTotalVisitors" Text="0" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Border Width="48" Height="48" Background="#2a3a5a" CornerRadius="12" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </DockPanel>
            </Border>
            <Border Style="{StaticResource SummaryCardStyle}" Background="#3a1e0a" Margin="8,0,0,0">
                <DockPanel VerticalAlignment="Center">
                    <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                        <TextBlock Text="Peak Hour" Foreground="#bb8866" FontSize="13"/>
                        <TextBlock x:Name="lblPeakHour" Text="--" Foreground="White" FontSize="36" FontWeight="Bold" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Border Width="48" Height="48" Background="#5a3010" CornerRadius="12" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="📈" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </DockPanel>
            </Border>
        </UniformGrid>

        <Border Grid.Row="2" Background="#141416" CornerRadius="15" Padding="10" Margin="0,0,0,10">
            <DataGrid x:Name="dgAttendanceReport" 
                      Style="{StaticResource ModernDataGrid}"
                      IsReadOnly="True"
                      RowHeaderWidth="0"
                      RowHeight="52" 
                      FontSize="14"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time of Entry" Binding="{Binding CheckInTime}" Width="1.2*"/>
                    <DataGridTextColumn Header="Time Out" Binding="{Binding CheckOutTime}" Width="1.2*"/>
                    <DataGridTextColumn Header="Member Name" Binding="{Binding Name}" Width="2*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Membership Plan" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding MembershipType}" 
                                                Style="{StaticResource MembershipPlanBadge}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>