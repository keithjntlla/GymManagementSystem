<UserControl x:Class="GymManagementSystem.Views.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <Grid Margin="0,20,0,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Reports" Foreground="White" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="View and analyze gym performance metrics" Foreground="Gray" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,0">
            <RadioButton Content="Financial"   Tag="💰" IsChecked="True" Style="{StaticResource TabButtonStyle}" Click="TabFinancial_Click"/>
            <RadioButton Content="Attendance"  Tag="📊" Style="{StaticResource TabButtonStyle}" Click="TabAttendance_Click"/>
            <RadioButton x:Name="rbTabExpirations" Content="Expirations" Tag="⏰" Style="{StaticResource TabButtonStyle}" Click="TabExpirations_Click"/>
            <RadioButton Content="Refunds"     Tag="↩" Style="{StaticResource TabButtonStyle}" Click="TabRefunds_Click"/>
            <RadioButton Content="Discounts"   Tag="🏷️" Style="{StaticResource TabButtonStyle}" Click="TabDiscounts_Click"/>
        </StackPanel>
        <Separator Grid.Row="1" VerticalAlignment="Bottom" Background="#333"/>

        <Frame x:Name="ReportsFrame" Grid.Row="2" NavigationUIVisibility="Hidden" Margin="0,20,0,0"/>

    </Grid>
</UserControl>
