<UserControl x:Class="GymManagementSystem.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <UserControl.Resources>
        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Background="Transparent">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                                <TextBlock Text="{TemplateBinding Tag}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                            <Border x:Name="HighlightBar" Height="3" Background="#ff6b00"
                                    VerticalAlignment="Bottom" Visibility="Collapsed" CornerRadius="1.5"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter TargetName="HighlightBar" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,25">
            <TextBlock Text="System Settings" Foreground="White" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Configure gym rates, user accounts, and backups" Foreground="Gray" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <RadioButton Content="General" Tag="⚙" Style="{StaticResource TabButtonStyle}" Click="TabGeneral_Click"/>
            <RadioButton Content="Rates &amp; Plans" Tag="₱" IsChecked="True" Style="{StaticResource TabButtonStyle}" Click="TabRates_Click"/>
            <RadioButton Content="User Accounts" Tag="👤" Style="{StaticResource TabButtonStyle}" Click="TabUsers_Click"/>
            <RadioButton Content="Backup" Tag="💾" Style="{StaticResource TabButtonStyle}" Click="TabBackup_Click"/>
        </StackPanel>
        <Separator Grid.Row="1" VerticalAlignment="Bottom" Background="#333"/>

        <Frame x:Name="SettingsFrame" Grid.Row="2" NavigationUIVisibility="Hidden" Margin="0,20,0,0"/>
    </Grid>
</UserControl>