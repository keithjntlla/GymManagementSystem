<UserControl x:Class="GymManagementSystem.Views.MainViews.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GymManagementSystem"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="0,20,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Payment Processing" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock Text="Search Member" Foreground="Gray" Margin="0,0,0,5"/>
                <Grid>
                    <TextBox x:Name="txtSearch" Background="#1a1a1c" Foreground="White" BorderBrush="#333" Padding="35,12,10,12" 
                             FontSize="14" TextChanged="TxtSearch_TextChanged" GotFocus="TxtSearch_GotFocus"/>
                    <TextBlock Text="🔍" Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="12,0,0,0" IsHitTestVisible="False"/>
                    <Popup x:Name="popSearch" PlacementTarget="{Binding ElementName=txtSearch}" Placement="Bottom" StaysOpen="False" Width="{Binding ElementName=txtSearch, Path=ActualWidth}">
                        <Border Background="#222" BorderBrush="#333" BorderThickness="1" CornerRadius="0,0,10,10">
                            <ListBox x:Name="lstSearchResults" Background="Transparent" BorderThickness="0" Foreground="White" MaxHeight="200" SelectionChanged="LstSearchResults_SelectionChanged">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="10,5">
                                            <TextBlock Text="{Binding FullName}" FontWeight="Bold" FontSize="14"/>
                                            <TextBlock Text="{Binding MemberID}" Foreground="Gray" FontSize="12"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                    </Popup>
                </Grid>
            </StackPanel>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#141e3c" CornerRadius="15" Margin="0,0,10,0" Padding="20" MinHeight="250">
                    <Grid>
                        <StackPanel x:Name="panelMemberInfo" Visibility="Collapsed">
                            <TextBlock Text="Member Information" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                            <Border Width="100" Height="100" Background="#1a1a1c" CornerRadius="50" HorizontalAlignment="Center" Margin="0,0,0,15">
                                <Image x:Name="imgMemberPhoto" Stretch="UniformToFill">
                                    <Image.Clip>
                                        <EllipseGeometry Center="50,50" RadiusX="50" RadiusY="50"/>
                                    </Image.Clip>
                                </Image>
                            </Border>
                            <TextBlock x:Name="lblMemberName" Text="Name" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="lblMemberID" Text="ID" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,20"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Phone" Foreground="Gray" FontSize="12"/>
                                    <TextBlock x:Name="lblMemberPhone" Text="-" Foreground="White" Margin="0,2,0,10"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Gender" Foreground="Gray" FontSize="12"/>
                                    <TextBlock x:Name="lblMemberGender" Text="-" Foreground="White" Margin="0,2,0,10"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                        <StackPanel x:Name="panelNoMember" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="👤" FontSize="48" Foreground="#333" HorizontalAlignment="Center"/>
                            <TextBlock Text="Select a member to view details" Foreground="Gray" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Background="#2d1441" CornerRadius="15" Margin="10,0,0,0" Padding="20" MinHeight="250">
                    <StackPanel>
                        <TextBlock Text="Membership Rates" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                        <ItemsControl x:Name="icRates">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Margin="5" Padding="10" Background="#1a1a1c" BorderBrush="#333" 
                                            Click="Rate_Click" Tag="{Binding}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding PlanName}" Foreground="Gray" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding Price, StringFormat=₱\{0:N0\}}" Foreground="White" 
                                                       FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="3" Background="#1a1a1c" CornerRadius="15" Margin="0,20,0,0" Padding="20">
                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Payment Mode" Foreground="Gray" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="rbCash" Content="Cash" Foreground="White" IsChecked="True" Margin="0,0,20,0"/>
                                <RadioButton x:Name="rbGCash" Content="GCash" Foreground="White"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Amount Paid" Foreground="Gray" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtAmountPaid" Background="#141416" Foreground="White" BorderBrush="#333" Padding="10" FontSize="16" TextChanged="TxtAmountPaid_TextChanged"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#1e2a4a" CornerRadius="10" Padding="15" Margin="0,0,10,0">
                            <StackPanel>
                                <TextBlock Text="Total Amount" Foreground="Gray" FontSize="12"/>
                                <TextBlock x:Name="lblTotalAmount" Text="₱0.00" Foreground="#4a90e2" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="#1e3a2a" CornerRadius="10" Padding="15" Margin="5,0,5,0">
                            <StackPanel>
                                <TextBlock Text="Amount Paid" Foreground="Gray" FontSize="12"/>
                                <TextBlock x:Name="lblAmountPaidDisplay" Text="₱0.00" Foreground="#2ecc71" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="#3a1e4a" CornerRadius="10" Padding="15" Margin="10,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Change" Foreground="Gray" FontSize="12"/>
                                <TextBlock x:Name="lblChange" Text="₱0.00" Foreground="#9b59b6" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Date of Transaction" Foreground="Gray" Margin="0,0,0,5"/>
                            <Border Background="#141416" BorderBrush="#333" BorderThickness="1" Padding="10">
                                <TextBlock x:Name="lblTransactionDate" Text="-" Foreground="White"/>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="New Expiry Date" Foreground="Gray" Margin="0,0,0,5"/>
                            <Border Background="#141416" BorderBrush="#333" BorderThickness="1" Padding="10">
                                <TextBlock x:Name="lblNewExpiryDate" Text="-" Foreground="White"/>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Button x:Name="btnProcessPayment" Content="Process Payment" Padding="0,15" Background="#3e4a5e" Foreground="White" FontSize="18" FontWeight="Bold" BorderThickness="0" Click="ProcessPayment_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>